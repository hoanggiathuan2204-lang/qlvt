import 'package:flutter/material.dart';

class AppColors {
  AppColors._();

  // ─── Brand ───────────────────────────────────────────────
  static const Color primary = Color(0xffF57C00); // Cam chủ đạo
  static const Color secondary = Color(0xffFF9800); // Cam nhạt hơn
  static const Color accent = Color(0xffFFB300); // Vàng cam

  // ─── Sidebar ─────────────────────────────────────────────
  static const Color sidebar = Color(0xff1C1C2E); // Tối đậm
  static const Color sidebarHover = Color(0xff2D2D44); // Hover
  static const Color sidebarText = Colors.white70;

  // ─── Background ──────────────────────────────────────────
  static const Color background = Color(0xffF5F6FA);
  static const Color surface = Colors.white;

  // ─── Text ────────────────────────────────────────────────
  static const Color text = Color(0xff1F2937);
  static const Color subText = Color(0xff6B7280);

  // ─── Card ────────────────────────────────────────────────
  static const Color card = Colors.white;
  static const Color border = Color(0xffE5E7EB);

  // ─── Status ──────────────────────────────────────────────
  static const Color success = Color(0xff22C55E);
  static const Color warning = Color(0xffF59E0B);
  static const Color danger = Color(0xffEF4444);
  static const Color info = Color(0xff3B82F6);

  // ─── Dashboard card colours ───────────────────────────────
  static const Color blue = Color(0xff3B82F6);
  static const Color green = Color(0xff22C55E);
  static const Color orange = Color(0xffF57C00);
  static const Color purple = Color(0xff8B5CF6);
  static const Color teal = Color(0xff14B8A6);
  static const Color red = Color(0xffEF4444);
}
